NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Outcome-Aware: mirror of core OA_FEATURE_ENABLED. When "true" the per-profile
# OA toggle is interactive; otherwise it is shown disabled.
NEXT_PUBLIC_OA_ENABLED=false
